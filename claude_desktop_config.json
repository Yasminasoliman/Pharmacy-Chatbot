{
  "_comment": "Add this block to your Claude Desktop mcpServers config (~/.config/claude/claude_desktop_config.json on Linux, ~/Library/Application Support/Claude/claude_desktop_config.json on macOS)",
  "mcpServers": {
    "pharmacy": {
      "command": "python",
      "args": ["<ABSOLUTE_PATH_TO_PROJECT>/app/mcp/server.py"],
      "env": {
        "DATABASE_URL": "postgresql://postgres:YOUR_PASSWORD@localhost:5433/pharmacy_ai",
        "PHARMACY_API_URL": ""
      }
    }
  }
}
